# Core ML & DL libraries
torch>=2.0.0
transformers>=4.40.0
tokenizers>=0.13.0
huggingface-hub>=0.16.0
sentencepiece>=0.1.98

# Hyperparameter tuning / training
optuna>=3.0.0

# ML / NLP
scikit-learn>=1.5.0
spacy>=3.7.0
nltk
regex
joblib                 # <--- Wajib untuk memuat model .joblib
symspellpy>=6.7.7

# Data
pandas>=2.1.0
numpy>=1.25.0

# Google Cloud STT & Autentikasi
google-cloud-speech    # <--- Wajib untuk Speech-to-Text
google-auth            # <--- Wajib untuk autentikasi

# Web framework
fastapi
pydantic>=1.10.0
uvicorn[standard]
python-multipart       # <--- Wajib untuk UploadFile di FastAPI

# Audio Processing (Jika digunakan di Python, tapi librosa/soundfile tidak digunakan di app.py)
# Jika tidak digunakan di app.py, Anda bisa menghapusnya untuk menghemat ukuran image Docker
librosa
soundfile

# Production server
gunicorn